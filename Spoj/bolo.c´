#include <iostream>
using namespace std;

int main(){
	int a,b,c,trigo,ovos,leite;
	cin >> a >> b >> c;
	trigo = a/2;
	ovos = b/3;
	leite = c/5;
	if(a<b && a<c) cout << a << endl;
	else if(b<a && b<c) cout << b << endl;
	else cout << c << endl;
	return 0;
}
